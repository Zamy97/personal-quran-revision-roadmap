export const environment = {
  production: false,
  /**
   * Mushaf PDF URL. Empty = local assets file for ng serve.
   * Set to a CORS-enabled HTTPS URL (R2, S3, jsDelivr, etc.) when hosted remotely.
   */
  mushafPdfUrl: ''
};
