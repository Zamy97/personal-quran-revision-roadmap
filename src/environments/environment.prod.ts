export const environment = {
  production: true,
  /**
   * Remote mushaf PDF (must allow CORS + range requests for PDF.js).
   * Leave empty only if the PDF is shipped in /assets/quran/.
   */
  mushafPdfUrl: ''
};
